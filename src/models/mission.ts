export interface IMission {
  id: number
  points: number
  totalCall: number
  name: string
  description: string
  institutionId: number
  institution: string
  tags: string[]
  createdAt: string
  updatedAt: string
  deletedAt: string
}
